import React from 'react';
import Terminal from './../';

export default class App extends React.Component {
  render() {
    return (
      <Terminal
        style={{
          flex: 1,
        }}
      />
    );
  }
}
